# docs/AGENTS.md

继承根规则。跨目录历史设计与实施记录见 [README.md](README.md)。

- 本目录只作历史追溯；已完成记录保留原文，在 README 标明状态与替代入口。
- 新代码设计进 `docs/design/history/`，当前实验计划进 `experiments/plans/`，开题材料进 `docs/thesis/`；一次性材料放 `tmp/`，不进入 Git。
- 替代、移动或删除记录按根 §7.1 检查引用、唯一内容和恢复路径，不能仅因过时删除。
