# docs/design/history/AGENTS.md

继承 `docs/AGENTS.md`。本目录保存已完成的代码/跨目录设计与实施记录，只作历史追溯。

- 已完成记录保留原文；文中“当前”“下一步”、分支状态或旧目录路径均按文件日期读取。
- 实现与验证状态以 `code/INFRA_STATUS.md` 和证据台账为准，历史计划不覆盖当前计划、总纲或 runbook。
- 有效剩余任务迁入当前领域计划并给出替代入口，按根 §7 更新受影响的索引和日志。
- 替代、移动或删除记录按根 §7.1 检查引用、唯一内容和恢复路径，不能仅因过时删除。
