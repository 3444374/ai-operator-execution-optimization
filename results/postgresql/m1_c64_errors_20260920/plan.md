> 本文件是该次验证的运行前声明/实施规格原文，2026-09-21 从当时的主题计划（`docs/plans/data_organization_batching.md`）按“执行说明与测试材料放在一起”的原则移入。其中请求额度、停止条件和“本轮/下一步”均为当时记录；未使用的旧额度不构成今天继续运行的授权。

<a id="m1-c64-errors"></a>
## C64 异常诊断（2026-09-20）

状态：[本轮结果](../../results/postgresql/m1_c64_errors_20260920/README.md)为3,088次全部完成，C64四次通过；原故障未复现、根因仍待确定。下文保留执行前声明。

用户要求继续定位并处理上次两条未确认请求。原记录只能看到 `MODEL_UNAVAILABLE`，
不能区分底层连接、HTTP 客户端或观察器异常；先增加只含异常类型、原因链类型和代码位置的诊断。
保持原异常传播、请求期限和未确认资源处理，无重试，不记录异常消息或请求内容。

本次独立诊断最多 **3,088 次请求尝试、20 分钟**（含启动、核验与清理）：
PG/direct 各8行正确性，PG C4、C16 各512行一次，然后 PG C64 同一512行最多四次。
沿用上一节的模型、数据、资源、完整事件观测、90秒查询和30秒模型期限，固定行序；
C64 表示客户端最多64条在途请求。C4/C16 用于复现此前运行顺序，C64 首次及后三次全部保留。
这属于错误复现，不进行方法选点，不运行 C128、W、评价或图像阶段。
首个非预期错误立即停止；原账本与失败记录保留，不复用前次额度。若定位出可复现缺陷，
先用受控检查证明修复，再另行记录修复后的有限验证清单；不直接重发未确认请求。
