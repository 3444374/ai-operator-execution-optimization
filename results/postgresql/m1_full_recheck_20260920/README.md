# M1 完整容量复查与工程合并
## 结论与适用条件

本次复查恢复了原失败清单的四个常驻 gateway：32 个查询、16,400 次请求与服务端账目一致，
原两条未确认请求的故障未复现；容量扫描修复与异常诊断改动据此合入 main。
但 PG/direct 各容量均未满足持续供给要求，不能选出平台候选；原故障根因仍待确定，
不称已修复连接或模型缺陷。查询执行全部完成、控制器原清理扫描告警、随后只读资源核对通过
三件事分别报告，互不覆盖。


2026-09-20，用户要求再次复现原故障，检查后提交、合并并推送。
**完整容量清单的32个查询全部完成，合计16,400次请求与服务端一致，原未确认请求故障未复现。**
原控制器在结束扫描时记录一个短暂残留PID并返回失败；随后独立只读核对确认当前无残留，原告警保留。
本次将已验证的容量扫描修复与异常诊断改动合入main，不声称已经确定或修复原故障根因。
两条路径的选点状态均为 `inconclusive_supply`（持续供给不足，暂不能选点），不登记M1方法结论。

## 本轮范围与执行身份

按[运行前声明](../../../docs/plans/data_organization_batching.md#m1-full-recheck)，恢复原失败清单的四个常驻
PG gateway：C4、C16、C64、C128；每个容量均有PG与PG-source-direct对照。
每组512行，一次预热和三次交错测量；组列表为p4/p16/p64/p128/d4/d16/d64/d128，
四轮依次为原序、左移一位、倒序、左移两位，与原清单一致。
另有PG/direct各8行正确性；总上限16,400次/20分钟，容量阶段上限600秒。
第二个条件验证单元仅用于定位并修复具体缺陷之后，本轮未启动。

实际运行源码为 `7d585615`，模型、输入和运行文件SHA在启动前核对；
生产代码在运行期间未改动。模型为既有Qwen2.5-7B-Instruct，revision
`a09a35458c702b33eeacc393d103063234e8bc28`，单张4090 GPU0，BF16、张量并行1、context4096、
max-num-seqs128、batched-token8192、FCFS、分块输入处理、前缀缓存关闭、显存比例0.8。
vLLM0.25.1与driver隔离，无安装或下载；复用此前诊断PG18.3扩展，未修改C/SQL/wire。

同一512行电影调参输入、相同行序，二值分类、temperature0、最大输出128；不使用评价分区。
统一L128、Core输入/结果各128MiB、PG留存256MiB/暂存4MiB、完整事件观测，
模型期限30秒、查询期限90秒。新目录、新账本、无重试，不沿用以前的未用额度。
[运行身份、预检和配置](raw/run-summary.json.gz)与[源码/编排](raw/source-record.json.gz)可逐项核对。

## 全部查询与质量原值

下表每格按预热、测量一、二、三排列；时间包含完整调用至EOF，单位为秒，质量列分母均为512。
所有32个查询均完成512行，关联核对通过、missing0、invalid0；模型分类误判保留在分母。

| 路径与容量 | 完整耗时：预热 / 三次测量（秒） | 分类正确行：预热 / 三次测量 |
|---|---|---|
| PG C4 | 14.501 / 14.516 / 14.519 / 14.403 | 434 / 434 / 434 / 434 |
| PG C16 | 9.733 / 9.348 / 9.979 / 9.755 | 433 / 433 / 433 / 434 |
| PG C64 | 12.155 / 11.221 / 11.518 / 11.256 | 435 / 433 / 434 / 434 |
| PG C128 | 12.995 / 12.320 / 12.543 / 12.355 | 435 / 434 / 434 / 434 |
| direct C4 | 11.803 / 11.897 / 11.873 / 11.819 | 435 / 435 / 435 / 435 |
| direct C16 | 5.901 / 5.503 / 5.975 / 5.737 | 433 / 434 / 433 / 433 |
| direct C64 | 4.213 / 4.285 / 4.236 / 4.247 | 433 / 434 / 434 / 433 |
| direct C128 | 5.481 / 5.174 / 5.938 / 5.053 | 434 / 434 / 433 / 434 |

[完整原值](raw/completed-queries.json.gz)保留质量、输出变化数量、资源、供给、等待位置与选择器结果。
temperature0不作为输出完全确定的证明；每组各次输出变化和配对差异均保留。
PG各容量的三次测量均未满足原计划的持续供给要求；direct C4/C16满足，C64/C128未全部满足。
选择器阈值未改：中段60%内，至少80%的时间须达到ceil(0.9×C)个HTTP在途请求。
因此PG和direct都未选出平台候选；观察最快的容量分别为C16和C64，不能据此替代合格选点。
没有运行W调参、独立评价、观测开销对照或图像阶段，也不将短查询登记为60秒稳态结果。

## 原故障、请求计数与清理告警

本轮16,400次发送前记账、服务访问记录和最终success counter一致。
C64与C128各完成四次512行查询；没有 `core_http_error` 或 `core_uncertain`。
PG四组共8,192个HTTP开始/结束、Core终态和Map交付，16次Job排空；
[事件计数](raw/event-counts.json.gz)与[完整公开事件](raw/events.jsonl.gz)保留原始对应关系。
[服务观测](raw/service-observations.json.gz)在结束时记录running/waiting均0。

控制器耗时505.869秒，未超时或超额。四个gateway均退出0，transport关闭、逻辑占用归零且socket移除；
模型正常退出0，未强制终止，PG pid文件已移除，ACL恢复。
但控制器最后按命令行中的路径子串扫描进程，记录PID301008，因而原 `summary.status` 为 `failed`。
它没有保存该PID的命令快照，无法事后确认其身份；不能直接认定是模型残留，也不能认定一定是并行监测进程。

随后未执行kill、未重发模型请求，仅做独立只读核对：该PID已不存在，匹配任务目录的进程为空，
18092/57736无监听，两GPU均1MiB、0%且无计算进程，四个gateway的正常退出记录完整。
`final-cleanup.json`记录这次核对通过，原控制器告警仍在[运行摘要](raw/run-summary.json.gz)中，未被覆盖。
因此本报告区分“查询执行全部完成”“控制器原清理扫描告警”和“随后资源核对通过”，不把整份原控制器报告改写成无告警成功。

此前两条未确认请求的根因仍待确定；本轮没有新的真实异常样本，不猜测并修改连接或重试策略。
本次合并的是具有受控证据的容量扫描修复和异常类型记录能力，旧失败继续作为未确定原因的历史记录。

## 验证与保存

Linux provider本轮79项：70通过、9项依环境跳过，包含全部三个新增传输诊断测试。
容量扫描改动的386项调度、81项图像fixture及实际PG/HTTP集成证据见
[原修复报告](../m1_supply_followup_20260920/README.md)；异常记录的先失败后通过检查见
[诊断报告](../m1_c64_errors_20260920/README.md)。本轮未修改这些实现，未重复无关测试。

公开原始记录使用7份gzip与[字节/SHA清单](raw/manifest.json)，解压逐字节核对；
检查和服务日志保存在[日志归档](raw/checks-and-service-logs.json.gz)。
原始输入、输出、私有事件、数据库目录、以前的失败及本次清理告警继续保留。
合并前检查本次文字、Markdown本地链接、差异格式，以及暂存内容和解压证据的隐私。
