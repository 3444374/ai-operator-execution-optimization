> 本文件是该次验证的运行前声明/实施规格原文，2026-09-21 从当时的主题计划（`docs/plans/data_organization_batching.md`）按“执行说明与测试材料放在一起”的原则移入。其中请求额度、停止条件和“本轮/下一步”均为当时记录；未使用的旧额度不构成今天继续运行的授权。

## 当前实施：执行修复与数据库查询套件（2026-09-10）

### 异步期限与输入/交付节奏诊断（2026-09-14）

**本轮状态**：[修订与诊断报告](../../../results/postgresql/async_deadline_flow_20260914/README.md)。
期限与时序工具回归、正常PG 2138项TAP、12个单元96次fixture诊断完成。
真实模型首轮24次后因active work2048<context4096的准备错误停止；用户再批准33次，修正为4096后全部完成。
累计57次生成POST：56条正常结果逐行关联通过，1条真实direct结果的末次写入跨期限后保持失败并被拒绝评分。
所有8行正常单元输出一致，均有2个情感假阴性；四次真实flow暂停时队首均未就绪，没有区分ready-first的目标分支，不作收益归因。
原账本32预留/24实际及失败保留，补跑独立33预留/33实际；补跑228.211秒、进程/GPU/PG/ACL清理通过。


本轮从 `2b7824b7` 处理复审新增问题。容量等待、零任务Map与普通PG小结果发送缓冲已完成验证，
不重复重构或运行同一缓冲实验。范围为共同异步记录器的期限判断，以及现行PG/gateway输入与交付节奏的受控诊断。
初始受控阶段真实模型POST上限0；追加授权的64次真实模型范围见下文，不据旧版本1,152次请求给当前版本排名。

- 期限修订的依据为当前 `record_async_execution`、`DirectMap.rows` 与
  [Python 3.12 asyncio.Timeout](https://docs.python.org/3.12/library/asyncio-task.html#asyncio.Timeout)。
  保留既有取消/清理结构，用同一event-loop单调时钟核对原timeout的绝对期限，覆盖上下文进入、行记录前后与EOF。
  保留声明期限、实际发现时点、取消触发、查询终止与清理事实；晚发现不回填为期限时已取消。
  已写结果和摘要不删除，失败不进入评分。同步记录、外部取消和清理失败合同维持原有含义。
- 定向验证使用正常快速/协作式等待、阻塞源/热迭代器/EOF、最后一行阻塞写入及真实DirectMap迭代控制流，
  并检查慢清理和首错。临时fixture无模型或外部服务访问；失败保留并修复受影响路径。
- 物理节奏先测当前策略：区分输入读取/准备、PG接纳、Core实际提交、PG结果就绪/向上层返回和应用收到。
  明确首次poll开启派发与补窗口优先的候选收益/等待取舍；不直接把它们定为正确性错误。
  仅增加诊断所需的可选观测，默认执行策略保持不变。受控服务、有限输入与停止条件在具体运行前补齐。
- 具体诊断采用 `SEMLOOM_FLOW_DIAGNOSTIC=1` 的独立PG测试构建：追加无payload的单调时钟事件与有界输入pull暂停，
  并在同一测试构建中比较现行次序和ready-result-first变体。正常构建不含这些字段、GUC、追踪或替代策略。
  来源为 `semloom_pump_window_next/read_total/read`、gateway首次advance开启派发及现有Core事件；这是控制流诊断的工程选择。
  固定8个非NULL合成Map输入，C4/L4、PG总留存8MiB/暂存4MiB，rows候选4、组最多4、work上限2048；
  第0个fixture请求延迟120ms，其余2ms。输入pull按无暂停、首次窗口末项（index3）暂停80ms、后续补充（index4）暂停80ms，
  与现行/ready-first组合成6配置，每配置两次交错运行，共96 fixture POST；诊断总上限128、真实模型0。
  每单元statement timeout10秒、8行输出均完整核对；失败保留并停止该轮，不调整时间或窗口来强求差异。
  记录PG child开始/返回、task准备/接纳、结果就绪/节点返回/行释放、Core实际提交和客户端接收；同机CLOCK_MONOTONIC明确登记。
  注入暂停只代表可控慢源，不声称真实I/O或质量/吞吐收益；对照保持相同观测与预算，普通PG缓冲四查询不重跑。
  正常与诊断PG扩展分别安装到隔离prefix；正常构建需编译、PGXS/TAP及既有选定PG回归，诊断构建运行上述有限对照。
  复用已验证driver与PG18.3，不下载/运行模型；数据、日志与PGDATA放新数据盘目录，测试后关闭进程/socket并恢复ACL。
- 用户在本轮处理中追加允许服务器真实模型测试。新独立上限64次模型POST、单GPU、从模型身份核验/服务准备至清理20分钟；
  使用既有Qwen2.5-7B-Instruct/vLLM0.25.1、BF16/TP1/context4096和现成Movie原始输入前8行，重新核对source/model/data身份。
  正常PG预热8、验证8、PG-source direct验证8；诊断PG现行/ready-first在index4暂停80ms各两次×8行（32请求）；
  再用1行真实direct结果做末次同步记录跨5秒期限的受控过期检查，合计57次，余量不自动使用。
  不要求真实模型像fixture一样按指定顺序完成；若没有就绪头部被输入等待阻挡，只报告未观察到，不调整输入/预算强求效果。
  8个模型单元前各执行一次已确认的prefix cache清空管理请求，另计8次，不属于生成POST；
  单元参数与停止条件保持固定；首个非预期失败停止后续模型单元，不重试/退款。完整请求、行绑定、结果和质量差异均保留。
  timeout边界单元只允许预期的TimeoutError并要求真实行已完整记录、发现时点晚于声明期限、资源清理；不计入正常成功耗时。
  此为小规模真实性/正确性验证，不是当前版本性能曲线；正式同资源重复/强静态和原生系统资格仍另行设计。
- 后续模型比较仍需同资源交错重复及强静态对照；请求数FIFO→token表征FIFO→简单重排分别识别表征、预算与排序成本。
  完整诊断与低扰动观察需单独对照，原生系统质量及E/F不由本轮替代。
