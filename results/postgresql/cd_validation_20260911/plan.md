> 本文件是该次验证的运行前声明/实施规格原文，2026-09-21 从当时的主题计划（`docs/plans/data_organization_batching.md`）按“执行说明与测试材料放在一起”的原则移入。其中请求额度、停止条件和“本轮/下一步”均为当时记录；未使用的旧额度不构成今天继续运行的授权。

2026-09-11 C/D复核：从 `d2f9cc13` 对照用户针对 `c4354cd6` 的审查；A/B公共修订已完成，
本次先补D组织审计，不重新修改PG/Core执行机制。来源为现有 `SessionEngine._dispatch/_terminal`、
`BoundedAsyncBackend.poll`、`organization_evaluation.verify_organization` 与固定的既有C/D事件。
架构§8.7–8.8的完成/消费/连接职责分开原则继续适用；不读取或外发公司私有材料。

| 本次对象 | 实现决定 | 验证条件 |
|---|---|---|
| 活跃work独立核对 | 从`core_submitted`建立责任，只有`core_terminal`解除；逐个核对事件上的work/request快照 | 缺失事件/快照、少报work、重终态、未知终态、将HTTP结束/取消/交付误作释放均拒绝 |
| 组织成员和顺序 | 完整成员索引/组大小与原始提交事件顺序分别校验；不排序后掩盖差异 | 交换组内提交、交错/交换分组、跨组错配拒绝；HTTP起始次序单独报告，不假设必须等同提交序 |
| 机制与历史解释 | `service_quantum`说明由下游决定物理执行；当前Map组展开为独立请求，无整组完成屏障 | 保留rows/work分组不同但执行可能相同的事实；计算真实D受C限制的work上界，明确4096不可能生效 |
| 下一步实验准备 | C同L/B/S对照；D在相同C/L/字节范围比较可生效与不生效work上限；观测源/输出等待与准备成本 | 先固定输入、参数、停止条件、准备/常驻/查询计时；未获新模型额度不运行；数据或供给不足不自动调参 |

本次审计使用独立工作副本与原始事件只读重放。旧9份组织轨迹已通过新审计，516项责任重建/结算一致，
另记重放结果和新审计版本，不改写原运行时间、预测或原评估报告。重放不等于新增真实模型验证。


### C/D补充真实诊断（2026-09-11，新授权）

**已完成**：[修订与新结果](../../results/postgresql/cd_validation_20260911/README.md)。
16单元1,152/1,152模型POST有效，319.113秒内模型正常退出，随后PG/ACL清理。
旧9轨迹及新12组织轨迹通过新审计；C同配置未显示total更快，D紧预算确实限制提交但三组均较慢。
保留rows紧预算1行输出差异；下一步是重复和成本消融，不将本次单点扩写为性能结论。下列为已执行的原规格。

用户在审计修订过程中明确允许加入真实模型测试。本轮独立上限：**1,152次模型`/chat/completions` POST、
单张RTX4090、从模型文件核对/服务准备到清理最多20分钟**；不复用C288、D408或A/B旧额度。
8个配置各16行预热、128行验证，共16个单元；每个单元只有一次，不作统计排名。
模型与source身份核验、服务健康、全部单元以及清理均有独立记录。首次执行有效性失败或达到上限即停止，
不自动重试、不退款、不在线改配置。模型答错进入质量分母；非法任务输出停止后续单元并保留诊断。

- 同一Movie原始前128个行出现，预热取前16，不按标签选样；复用原SemBench生成资产并重新核验SHA、
  schema与完整消息token数。模型固定Qwen2.5-7B-Instruct revision `a09a35458c702b33eeacc393d103063234e8bc28`、
  vLLM0.25.1、BF16/TP1/context4096、原生FCFS、max_num_seqs128/max_num_batched_tokens8192、GPU利用率0.8。
  prefix cache开启、每单元前清空并核对；16个cache reset是独立管理POST，不属于模型推理POST。
- C两配置：equal/total，**均C8、L32、PG B8MiB、S4MiB、Core输入128MiB/结果64MiB**。
  保持组织关闭，比较同一可完成条件下的查询与内存；若equal发生容量拒绝，保留为该配置可用性失败，
  不用部分结果估吞吐、不调高预算重跑。旧受控宽行同配置反例继续作为可用性依据。
- D六配置：rows/work/length × W4096/W16384；**均C32、L64、PG total B8MiB/S4MiB、Core输入128MiB/结果64MiB**；
  候选64、每组最多8行、work目标1024。所有D配置都作相同token化、事件记录和逐行独立HTTP。
  先验证128行中最高32个work之和大于4096、小于等于16384；context4096不随W改变，保留单项可接纳检查。
  W16384为不受工作量限制的对照；W4096必须实际出现“请求数尚有余量，但工作量不足”的容量拒绝，
  否则本轮D接纳因果结论为inconclusive，不自动换数据、升C或降W。rows/work不因组数不同被称作两种物理批执行。
- 在已有Core sink上增加默认关闭的容量拒绝观测；本轮组织adapter启用，区分flow eligibility与选中成员dispatch。
  快照与独立活跃责任逐项匹配后，按requests/work/both归因；计数是容量检查被拒次数，含重复探测，不是阻塞秒数。
  历史无此观测的轨迹保持unknown，不从组数或峰值猜拒绝次数。只增加观察，不改变can_dispatch、排序或资源归还。
- 预热顺序：C equal/total，D rows宽松/紧，work宽松/紧，length宽松/紧；验证反转每对先后顺序，
  仍仅各一轮。每单元保存实际候选数、submit与HTTP顺序、work/request峰值和拒绝类型、PG留存/暂存、
  tokenizer准备次数与累计时间、gateway常驻RSS、首行可见时间、JCT。额外报告backend终态到应用消费的
  积压与等待区间；该区间包含gateway/传输/PG恢复输入序/消费者，不冒充纯PG已就绪行计数。
- 运行前先通过本地与Linux受影响回归、既有轨迹重放和真实PG/受控HTTP组织案例；不要求新模型替代这些反例。
  旧C/D预测和耗时原样保留；此次同配置C及D预算生效检查分别形成新记录，不与旧值混成重复或稳态曲线。
