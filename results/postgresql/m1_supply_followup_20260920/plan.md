> 本文件是该次验证的运行前声明/实施规格原文，2026-09-21 从当时的主题计划（`docs/plans/data_organization_batching.md`）按“执行说明与测试材料放在一起”的原则移入。其中请求额度、停止条件和“本轮/下一步”均为当时记录；未使用的旧额度不构成今天继续运行的授权。

<a id="m1-supply-followup"></a>
## M1供给复测（2026-09-20）

状态：[本次结果](../../../results/postgresql/m1_supply_followup_20260920/README.md)保留容量扫描修复和失败。
真实C64预热时停止，账本1,552次尝试、服务端成功1,550次；未进入选点、测量重复或后续条件阶段。
测试资源已清理，不自动追加，修复与证据留在分支。下表保留运行前声明。

2026-09-20：用户再次授权进行M1测试。先按[上一轮结果](../../../results/postgresql/m1_m2_f_real_20260920/README.md)
定位PG路径的CPU与供给问题：零真实模型、固定25毫秒HTTP fixture、128行、并发4，常驻PG/direct各两次。
诊断信号为PG耗时同时超过direct的3倍和1秒额外时间；这是复现信号，不是方法选点条件。
随后按证据缩小复现、逐项核对并修复；真实复测的输入、服务、确切请求数和停止条件在启动前另列，
不继承前次未用额度，不改写前次供给不足或未运行的状态。

### 本次 M1 修复后的真实复测清单

沿用Qwen2.5-7B-Instruct的模型/处理器身份和上一轮单卡GPU0服务参数：BF16、TP1、context4096、
max-num-seqs128、batched-token8192、前缀缓存关闭。仅修复Core同次只读候选扫描重复汇总容量的开销，
任务表、资源责任与FIFO行为保留；受影响的调度、provider、图像和实际PG/HTTP fixture先复核。

- 使用既有电影隔离的完整512行调参、1024行评价分区，正确性取调参前8行；固定行序、标签与请求语义。
  上一轮已用过各分区的128/256行，本次不称全部评价输入从未被观察；选点只读调参结果，不按标签换样本。
- 全部阶段合计最多 **52,000次POST、60分钟**，完整分支最多51,216次，包含预热与正确性；
  各阶段新账本、无自动重试，未运行阶段不转移余额。真实运行不启用CPU profiler。
- 共用L128、Core输入/结果各128MiB、PG留存256MiB/暂存4MiB；增加C128需要先验证全部资源可达。
  所有点使用完整观测与相同诊断PG，查询期限90秒；模型质量误判留在分母，格式/关联错误立即停止。
- 供给比例、3%吞吐差异、10%相对极差和1+3轮要求均沿用上一轮定义。短查询只用于小规模选点，
  不宣布已达到60秒稳态资格；未获得候选时停止后续条件阶段，不换输入、增C、改阈值或追加重复。

| 阶段 | 固定单元 | 最大POST / 阶段时间 |
|---|---|---:|
| 正确性 | PG/direct各8行 | 16 / 120秒 |
| 容量筛查 | C4/16/64/128 × PG/direct，512行，1+3轮 | 16,384 / 900秒 |
| W调参，需PG平台候选 | 选定请求数、C128请求数（相同时不重复）、C128下W4096/8192/524288，512行，1+3轮 | 10,240 / 600秒 |
| 独立评价，需完成调参 | 选定请求数、C128请求数、不生效的descriptor、合格W；去除重复组，1024行，1+3轮 | 16,384 / 900秒 |
| 代表性观测开销，需选点 | 同一选定C的full/compact，1024行，1+3轮 | 8,192 / 600秒 |

W524288等于128×context4096；只有调参中确实触发work限制且所有配对吞吐满足声明差异的W才进入评价。
每轮次序预先由group列表生成：原序、左移1、倒序、左移2；首轮为预热，不从其中挑选最快重复。
模型/输入/源码SHA、实际POST与服务计数、供给时间线、全部质量与资源记录保留；结束清理自有服务和临时ACL。

2026-09-20：按用户选择完成[工作包 F 的工程与受控检查](#work-package-f)。
用户于 2026-09-20 要求尝试完成 M1、M2 与 F 的真实测试；下节小规模清单已执行，不沿用旧额度。
[结果](../../../results/postgresql/m1_m2_f_real_20260920/README.md)：文本8,248次、图像151次。M1未选出平台候选，后续条件阶段未运行；M2五组与F指定检查完成，测试资源已清理。
2026-09-20 按新审查意见调整 M1：旧等待位置实验作为
已完成的测量反例保留；当前问题、比较对象与选点规则以下节为准，旧 44,544 次运行表不再执行。
