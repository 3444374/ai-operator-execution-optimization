> 本文件是该次验证的运行前声明/实施规格原文，2026-09-21 从当时的主题计划（`docs/plans/data_organization_batching.md`）按“执行说明与测试材料放在一起”的原则移入。其中请求额度、停止条件和“本轮/下一步”均为当时记录；未使用的旧额度不构成今天继续运行的授权。

### 推进、空查询与发送缓冲复审（2026-09-12）

**服务器验证已完成**：[2026-09-14报告](../../../results/scheduling/capacity_wait_server_20260914/README.md)。
Linux/选定PG集成通过，262次fixture HTTP、真实模型0次。诊断脚本首轮写入失败已保留并修复；
修复后四次普通SQL验证支持发送缓冲假设。清理与830份代码/配置身份检查通过。
下列为本轮已执行的范围；不改变后续模型/性能比较的独立计划。

2026-09-14：用户已启动服务器并授权测试 `codex/capacity-wait-empty-map`，验证源码锁定 `cc0f54f6`。
本轮新增独立产物目录，执行 Linux 调度/provider/PG Python合同/实验工具回归，
PG三种组织模式的非空与空选择、总量留存与既有记录入口，以及上述四次普通SQL发送缓冲诊断。
模型 POST 上限仍为0；PG集成仅访问测试自己创建的localhost受控HTTP，最多2,500次fixture请求。
隔离PG集群与产物落数据盘；复用已有driver/PG18.3，不安装依赖、不下载模型或数据。
PG/fixture阶段最多20分钟，出现失败先保存并定位，不自动重跑或改变阈值；完成后关闭PG/socket/任务进程并恢复原ACL。
服务器验证结果另行登记；不把普通SQL缓冲观察推断为旧SemMap节点的完整分层归因。

2026-09-12本地阶段记录：针对 `3a097d36` 的三项剩余意见复现与修订；既有 A/B 公共修订和 D 生命周期/顺序审计保持已完成。
用户当时明确服务器未开机、暂不进行服务器测试，因此先只执行本地回归，PG诊断与空选择集成待执行；
这些未运行项已由上方2026-09-14记录更新。模型 POST 上限为 0，不启动排名或重复模型实验。
本地结果：[修订报告](../../../results/scheduling/capacity_wait_empty_map_20260912/README.md)。
定向27项通过，非空21轨迹1,380任务审核结果不变；完整本地结果和缺依赖/环境跳过见报告。
当时保留在修订分支；本轮服务器验证后按既有授权完成提交和合并检查。
来源与工程取舍：核对架构 §8.7–8.8 的完成/消费、lazy-open 职责；以该提交的
`SessionEngine.advance/_progress/_terminal`、`SchedulingSession._dispatch/advance`、
`SessionCapacity.can_dispatch` 与 `query_evaluation._evaluate_rows` 为自有实现依据；不读取公司材料。
保持 FIFO/既选组成员、容量公式、未知责任及后端拒绝退避；仅把计算容量等待与定时重试分开。
离线组织审计从已核验原始输入选择取得预期任务数，允许独立证明的零任务且未开流查询，非空仍要求完整结算。

验证：确定性时钟下覆盖 legacy/registered、组内 FIFO、容量未变/未恢复不反复派发、容量恢复立即推进、
无关唤醒及其他完成不解除后端退避；零任务/非零任务缺证据对照及 PG 空选择集成；受影响公共旧路径回归。
PG 诊断复用隔离 PG18.3，普通 SQL 慢速逐行产出，固定 128 行/每行 5 ms，仅改变返回字节（8/128 字节），
小/大交错各两次，独立记录服务端产出与客户端接收时间；最多 60 秒查询时间，失败停止并保留。
配置/源版本/preflight/全部结果与清理落入独立结果目录。先核对 REL_18_3 发送缓冲源码，
不增加逐行 flush、不改变 PG 协议或输入序；普通 SQL 对照不能直接证明旧 SemMap 各层耗时。
