# motivation/results/cpu/AGENTS.md

本目录保存真实模型 CPU-backed 端到端 baseline 和 CPU/GPU 对照报告。

## 规则

- 只能写成 CPU-backed baseline 或 CPU/GPU 对照。
- 不能把 CPU-only 结果写成 GPU-backed 结论。
- 若报告引用 GPU 结果，必须注明 GPU 原始 CSV 来源。
- 每个正式报告必须包含运行命令、参数、CSV、阶段计时、结果解释和不能声称的结论。
