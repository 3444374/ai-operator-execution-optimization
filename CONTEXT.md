# SemLoom Domain

本文件定义项目当前使用的领域术语。它只说明概念身份，不记录实现状态、运行参数或实验结论。

## Language

**SemLoom**:
面向数据库 AI 语义算子的工作量感知执行与多作业调度系统；它是整个系统的名称，不指某个算法、
数据库、分布式框架或模型服务。
_Avoid_: project、Project、DB-AIEL（作为系统名称）

**Database-Aware AI Execution Layer (DB-AIEL)**:
数据库语义算子与外部模型执行设施之间的架构层名称；SemLoom 是这一架构层的具体系统。
_Avoid_: 把 DB-AIEL 用作 Python 类型、函数或包名前缀

**AI semantic operator**:
由数据库拥有 SQL、计划、关系语义和查询生命周期，并将已编译语义任务交给 execution provider 的
数据库算子。
_Avoid_: 把逐行 UDF、外部 profiler 或普通 HTTP 调用直接称为数据库 AI 语义算子

**SemLoom execution provider**:
在中立 provider interface 后执行 SemLoom 工作描述、数据组织、准入、路由和多作业调度的方法实现。
_Avoid_: project provider、Project provider

**legacy Project identity**:
既有实验、配置和证据中以 `project_*` 或 `Project*` 表示本项目实现的历史身份；它只用于兼容和
可追溯性，不作为新接口的规范术语。
_Avoid_: 为统一品牌而重写历史实验身份

## 常用区分

以下区分在设计和指标说明中直接使用，不互相替代：

**行 / 工作项 / 请求 / Job**:
行是数据库记录；原始行号不一定唯一标识一次执行（重复原始 ID 保留、执行行号独立）。
工作项是某行参与某个算子形成的一次逻辑工作；请求是模型服务实际接收的一次调用；
Job 是调度归属单位，一个 Job 可含多个查询和流。同一行参与不同算子不是同一个工作项。

**组织批次 / 服务请求 / 模型迭代批次**:
组织批次是执行层把哪些工作项组织在一起（影响准备、排序和提交机会）；服务请求是模型服务
实际接收的一次调用；模型迭代批次是推理引擎在一次迭代中实际安排处理的序列和 token
（`max_num_seqs`、`max_num_batched_tokens` 约束的是这一种）。上游组织不直接决定引擎每次
迭代处理哪些 token；形成多少组不等于采用了多少种模型批处理方法。

**请求上限 / 实际在途量 / 服务端排队量**:
配置允许多少（C/K/W 等上限），不等于实际提交了多少（在途曲线），更不等于服务端排队或
GPU 正在计算多少。三者分别观测，不能互相推断。

**资源计费 / 进程内存 / 设备内存**:
逻辑预算（行/字节/work 计费）没有超额，只说明该逻辑域的计费未超限；不能自动说明整个
PG/gateway/Ray 进程的 RSS 或 GPU 显存受同样上限控制。逐进程峰值不能相加称同时峰值。

**取消请求 / 任务确认终止 / 资源释放**:
发出取消不等于计算停止；后端确认终态是另一事件；资源（名额、数据、张量）在该路径执行
协议规定的事件后才释放。三者不能都写成“取消完成”；无法确认完成前，不能仅因客户端断开
就把相关计算资源视为可复用。
