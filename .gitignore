.DS_Store

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
.pytest_cache/
.mypy_cache/
.ruff_cache/
.cache/
.coverage
htmlcov/

# Virtual environments
.venv/
.conda/
venv/
env/

# Local logs and temporary files
tmp/
*.log
*.tmp
*.swp
stdout
.worktrees/

# Machine-local runtime profiles can contain database credentials or tokens.
*.env
*.env.local
!*.env.example

# Machine-local SSH/remote helpers may embed external server hosts (AGENTS.md §10) -- never commit.
rd.py
rd_get.py

# Local model checkpoints and downloaded weights.
models/

# Local dataset payloads. Keep dataset metadata/readmes tracked, not raw dumps.
data/raw/**
!data/
!data/README.md
!data/raw/
!data/raw/README.md

# Claude Code local config (not for repo)
.codex/
.claude/
.agents/
.superpowers/
skills-lock.json
# CSV and Markdown summaries are tracked; Arrow payload dumps are reproducible outputs.
*.arrow
*.pdf
validation/results/fake_ai_embed_outputs/
validation/results/fake_ai_embed_outputs_smoke/

# Codex checkpoint/snapshot working artifacts (not for commit).
experiments/results/checkpoint_*/

# Full request/trace archives stay on local disk and AutoDL; Git keeps aggregates.
experiments/results/opening_database_e2e_text_refeed_20260808/raw.tar.gz
