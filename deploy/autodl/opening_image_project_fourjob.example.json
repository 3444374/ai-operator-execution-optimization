{
  "schema_version": 1,
  "formal": true,
  "experiment_id": "opening_image_project_fourjob_20260809",
  "output_root": "${IMAGE_PROJECT_FOURJOB_OUTPUT_ROOT}",
  "database_url": "${DATABASE_URL}",
  "ray_address": "${RAY_ADDRESS}",
  "job_manifest": "${IMAGE_FOURJOB_MANIFEST}",
  "policy_revision": "${IMAGE_PROJECT_POLICY_REVISION}",
  "model": "${IMAGE_MODEL_PATH}",
  "processor": "${IMAGE_MODEL_PATH}",
  "dtype": "float16",
  "batch_size": 64,
  "source_shards": 4,
  "cpu_workers": 16,
  "gpu_workers": 2,
  "max_active_batches": 32,
  "model_flops_per_image": "${IMAGE_MODEL_FLOPS_PER_IMAGE}",
  "gpu_peak_flops_per_s": "${IMAGE_GPU_PEAK_FLOPS_PER_S}",
  "source_queue_batches_per_job": 2,
  "warmup_rows": 64,
  "warmup_repeats": 1,
  "formal_repeats": 3,
  "schedule_seed": 20260809,
  "gpu_sample_interval_s": 0.5,
  "cpu_sample_interval_s": 0.25,
  "scenarios": [
    {"id": "single_short_full_pool", "policy": "static_partition", "jobs": ["short"]},
    {"id": "single_long1_full_pool", "policy": "static_partition", "jobs": ["long1"]},
    {"id": "single_long2_full_pool", "policy": "static_partition", "jobs": ["long2"]},
    {"id": "single_long3_full_pool", "policy": "static_partition", "jobs": ["long3"]},
    {"id": "fourjob_static_partition", "policy": "static_partition", "jobs": ["short", "long1", "long2", "long3"]},
    {"id": "fourjob_proposed", "policy": "proposed", "jobs": ["short", "long1", "long2", "long3"]}
  ]
}
