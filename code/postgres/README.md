# PostgreSQL semantic carrier

This directory contains PostgreSQL-owned semantic-operator carriers. The current implementation is
the `REL_18_3` PGXS capability spike in [`semloom_pg/`](semloom_pg/). A core patch directory must not
be added unless the carrier audit records a reproducible extension blocker.
