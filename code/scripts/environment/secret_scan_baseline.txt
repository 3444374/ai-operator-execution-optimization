# scan_git_secrets baseline allowlist
#
# Each non-comment line is a regex. A violation is suppressed if its file path
# OR its snippet matches any line here. Keep this list SHORT and specific --
# every entry is a reviewed false positive that the scanner will no longer
# block. Never use a broad pattern that could mask a real secret.
#
# Reviewed 2026-08-05: these are model-generated example URLs inside captured
# evidence (requests.csv output_text). The LLM produced a YAML config whose
# db_url uses the placeholder pair "user:password" against a real-looking
# DigitalOcean host. Not our infrastructure, not a real credential (literal
# user/password placeholders), and per the "do not modify evidence with no
# real leak" policy they are allowed verbatim.

ondigitalocean\.com

# Reviewed 2026-08-05: scanner implementation/test fixtures intentionally
# contain detector-shaped strings. These patterns match only the exact fixture
# lines, not whole paths, so a different token or credential remains blocking.
return "sshpass[ ]+-p with embedded password"$
_FAKE_HF = "hf_fakefakefakefakefakefake"
_FAKE_SK = "sk-proj-fakefakefakefakefake2025"
_FAKE_SK_CLASSIC = "sk-fakefakefakefakefake12"
self\.assertIsNotNone\(scan\.scan_line\('KEY="-----BEGIN (?:RSA )?PRIVATE KEY-----"'\)\)
self\.assertIsNotNone\(scan\.scan_line\(f"sshpass[ ]+-p \{_FAKE_PW\} ssh root@h"\)\)
self\.assertIsNotNone\(scan\.scan_line\("url=postgres://u:pw@10\.0\.0\.5:5432/db"\)\)
